import 'package:flutter/material.dart';

class SiteTaskDialog extends StatelessWidget {
  const SiteTaskDialog({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Text("Hello this isn't anything yet");
  }
}