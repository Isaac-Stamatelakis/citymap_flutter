import 'package:flutter/material.dart';

class DailySheetDialog extends StatelessWidget {
  const DailySheetDialog({super.key});
  @override
  Widget build(BuildContext context) {
    return const AlertDialog(
      content: Text("THERE HII")
    );
    
  }

}